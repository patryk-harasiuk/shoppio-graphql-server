// import { Order } from "../graphql/types.generated";

// export const formatPriceInOrder = (order: Order[]) => {
// 	return order.orderItems.map((item) => ({
// 		...item,
// 		product: { ...item?.product, price: item?.product.price.toString() },
// 	}));
// };
