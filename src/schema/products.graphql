extend type Query {
	products(first: Int!, skip: Int): [Product!]!
	product(id: ID!): Product
}

type Product {
	id: ID!
	name: String!
	slug: String!
	description: String!
	price: Int!
	createdAt: DateTime!
	updatedAt: DateTime!
	categories: [Category]!
}
